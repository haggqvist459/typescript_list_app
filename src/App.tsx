import { Header, Footer, AddItem, VisibilityFilters, ListItem } from "@/components";


function App() {

  return (
    <div className="bg-mint-light min-h-screen flex flex-col">
        <Header />
    </div>
  )
}

export default App

// header
// visibility filter
// the list
// the add item input 
// footer 
